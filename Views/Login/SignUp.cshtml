<div id="signup" class="jumbotron">
    <div class="container pt-5 pb-5">
        <form class="col-xl-6 col-lg-8 col-md-10 offset-xl-3 offset-lg-2 offset-md-1" onsubmit="return comparePW()">
            <h4>註冊</h4><br>
            <div id="error-message"></div>
            <input id="username" class="form-control" type="text" placeholder="帳號" required pattern="^(?=.*\d)(?=.*[a-z]).{8,30}$">
            <small>至少8個字以上，且須含有英文字母及數字。</small><br><br>
            <input id="password" class="form-control"  type="password" placeholder="密碼" required pattern="^(?=.*\d)(?=.*[a-z]).{8,30}$">
            <small>至少8個字以上，且須含有英文字母及數字。</small><br><br>
            <input id="password-confirm" class="form-control"  type="password" placeholder="確認密碼" required pattern="^(?=.*\d)(?=.*[a-z]).{8,30}$"><br>
            <button class="btn btn-secondary form-control" type="submit">註冊</button>
            <span class="align-bottom" ><a href="/login" onclick="inheritQueryString(this)">以現有帳號登入</a></span>
        </form>
    </div>
</div>

@section styles {
    <link rel="stylesheet" type="text/css" href="/ViewAssets/Login/signup.css">
}
@section footscripts {
    <script type="text/javascript" src="/ViewAssets/Login/signup.js"></script>
    <script type="text/javascript" src="/ViewAssets/Login/index.js"></script>
}